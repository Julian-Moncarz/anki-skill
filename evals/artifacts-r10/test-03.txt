

Here are the Anki cards I've generated from your memory allocation notes:

| # | Type | Front / Text | Back / Extra | Tags |
|---|------|-------------|-------------|------|
| 1 | basic | Memory allocation: Why is stack allocation faster than heap allocation? | Stack memory is contiguous, so allocation is just moving a pointer | memory-allocation, stack, performance |
| 2 | basic | Memory allocation: What causes a stack overflow? | Exceeding the fixed stack size, usually from deep recursion | memory-allocation, stack, errors |
| 3 | basic | Memory allocation: Why is heap allocation slower than stack allocation? | Memory fragmentation requires searching for free blocks | memory-allocation, heap, performance |
| 4 | basic | Memory allocation: Why can heap variables outlive the function that created them, while stack variables cannot? | Stack variables are tied to their function's scope and are popped on return; heap variables persist until explicitly freed | memory-allocation, stack-vs-heap |
| 5 | cloze | A memory leak occurs when {{c1::heap}} memory is allocated but never {{c2::freed}}. | | memory-allocation, heap, errors |
| 6 | basic | Memory allocation: Why do dynamically-sized data structures (linked lists, trees, resizable arrays) live on the heap? | Their size is unknown at compile time, so they need runtime allocation with flexible sizing | memory-allocation, heap, data-structures |
| 7 | basic | Memory allocation: What manages stack allocation — the programmer or the compiler? | The compiler manages it automatically | memory-allocation, stack |
| 8 | cloze | Stack variables have {{c1::deterministic}} lifetimes because they are tied to their enclosing scope. | | memory-allocation, stack |
| 9 | basic | Memory allocation: What happens to stack variables when a function returns? | They are popped off the stack (deallocated automatically) | memory-allocation, stack |
| 10 | basic | Memory allocation: In unmanaged languages, what must a programmer do after allocating heap memory with malloc/new? | Free it manually | memory-allocation, heap |

**Self-check notes:**
- Mirror check: Cards 9 and 7 are related but test different things (lifecycle vs. who manages). Cards 4, 8, and 9 all touch stack lifetimes from different angles (why vs. what property vs. what happens mechanically).
- Back brevity: All backs are under 10 words or are single short sentences.
- Distribution: ~40% why/how (1, 3, 4, 6), ~30% core concepts (5, 7, 8), ~20% mechanism (2, 9), ~10% application (10).
- No enumerations, no yes/no, no "give an example" cards.

These look good? I'll push them to Anki. Edit any you'd like to change.
