

Here are the Anki cards I've generated from the gradient descent content:

| # | Type | Front / Text | Back / Extra | Tags |
|---|------|-------------|-------------|------|
| 1 | basic | Gradient descent: What does the algorithm compute to determine the direction of parameter updates? | The gradient (partial derivatives) of the cost function | machine-learning, gradient-descent, process |
| 2 | basic | Gradient descent: Why are parameters updated in the *opposite* direction of the gradient? | The gradient points toward increasing cost; moving opposite minimizes it | machine-learning, gradient-descent, reasoning |
| 3 | cloze | The gradient descent update rule is: θ = θ − {{c1::α}} · ∇J(θ), where {{c1}} is the learning rate. | | machine-learning, gradient-descent, formula |
| 4 | basic | Gradient descent: What happens if the learning rate is too large? | The algorithm overshoots and may diverge | machine-learning, gradient-descent, reasoning |
| 5 | basic | Gradient descent: How does stochastic gradient descent (SGD) differ from batch gradient descent in what data it uses per update? | SGD uses one random example instead of the entire dataset | machine-learning, gradient-descent, sgd |
| 6 | basic | Why is SGD often preferred over batch gradient descent despite being noisier? | Faster per update and the noise helps escape shallow local minima | machine-learning, gradient-descent, sgd, reasoning |
| 7 | basic | Gradient descent: Under what condition on the cost function is it guaranteed to find the global minimum? | When the cost function is convex | machine-learning, gradient-descent, reasoning |
| 8 | basic | Gradient descent on a non-convex cost function can get stuck in what two types of problematic points? | Local minima and saddle points | machine-learning, gradient-descent, reasoning |

**Self-check notes:**
- **Mirror scan:** Cards 5 and 6 both involve SGD vs batch, but test different facts (data usage vs. why it's preferred). No mirror issues.
- **Back scan:** All backs are under 10 words or a single short phrase. Card 8 has "and" but it names two types of the same category (problematic points) — this is a single concept (where GD gets stuck), not two independent facts.
- **Distribution:** 5/8 are "why/how/reasoning" cards (~63%), 2 definitional, 1 formula — good mix.
- **Cloze scan:** Card 3's blank is not guessable from surrounding context.

These look good? I'll push them to Anki. Edit any you'd like to change.
