

I'll create flashcards from this TCP content. Let me identify the card-worthy facts and generate quality cards.

**Card-worthy content identified:**
- Three-way handshake purpose and steps (SYN → SYN-ACK → ACK)
- Why sequence numbers matter (ordering + missing segment detection)
- Failure behavior (timeout + exponential backoff)
- Connection closing (four-way handshake with FIN/ACK)

**Self-check notes:**
- Avoiding acronym expansion cards ("What does SYN stand for?")
- Avoiding enumeration of the 3 steps as a list — testing each step's purpose instead
- Checking for mirror pairs and back brevity
- Ensuring ~40%+ are "why/how" cards

Here are the cards:

| # | Type | Front / Text | Back / Extra | Tags |
|---|------|-------------|-------------|------|
| 1 | basic | TCP: Why do both sides exchange sequence numbers during connection setup? | To reassemble packets in order and detect missing segments | networking, tcp, handshake |
| 2 | basic | TCP: What does the client send in the first step of the three-way handshake? | A SYN packet with an initial sequence number | networking, tcp, handshake |
| 3 | basic | TCP: How does the server respond to a SYN packet during connection setup? | With a SYN-ACK: acknowledges the client's sequence number and provides its own | networking, tcp, handshake |
| 4 | basic | TCP: What is the purpose of the third step (ACK) in the three-way handshake? | Confirms the server's sequence number, completing mutual agreement | networking, tcp, handshake |
| 5 | basic | TCP: What happens when a handshake attempt fails? | The connection times out and the initiator retries with exponential backoff | networking, tcp, reliability |
| 6 | basic | TCP: How does connection teardown differ from establishment? | It uses a four-way handshake (with FIN and ACK packets) instead of three-way | networking, tcp, teardown |
| 7 | cloze | After a TCP connection is established, {{c1::either side}} can initiate data transfer. | | networking, tcp |

These look good? I'll push them to Anki. Edit any you'd like to change.
