Here are the Anki cards I've generated from the gradient descent content:

| # | Type | Front / Text | Back / Extra | Tags |
|---|------|-------------|-------------|------|
| 1 | basic | Gradient descent: Why does the parameter update move in the *opposite* direction of the gradient? | The gradient points toward increasing cost; moving opposite minimizes it | machine-learning, gradient-descent, reasoning |
| 2 | cloze | The gradient descent update rule is: θ = θ − {{c1::α}} · ∇J(θ), where {{c1}} is called the learning rate. | | machine-learning, gradient-descent, formula |
| 3 | basic | Gradient descent: What happens if the learning rate is too large? | The algorithm overshoots and may diverge | machine-learning, gradient-descent, reasoning |
| 4 | basic | Gradient descent: What happens if the learning rate is too small? | Convergence becomes painfully slow | machine-learning, gradient-descent, reasoning |
| 5 | basic | How does stochastic gradient descent (SGD) differ from batch gradient descent in data usage per update? | SGD uses one random example; batch uses the entire dataset | machine-learning, gradient-descent, sgd |
| 6 | basic | Why is SGD often preferred over batch gradient descent despite being noisier? | Faster per update and the noise helps escape shallow local minima | machine-learning, gradient-descent, sgd, reasoning |
| 7 | basic | Under what condition does gradient descent guarantee finding the global minimum? | When the cost function is convex | machine-learning, gradient-descent, reasoning |
| 8 | basic | Gradient descent on a non-convex cost function can get stuck in what two types of points? | Local minima or saddle points | machine-learning, gradient-descent, reasoning |

**Self-check results:**
- **Mirror scan:** Cards 3 & 4 (too large / too small α) — these are NOT mirror-deducible since the consequences are qualitatively different (divergence vs. slowness), not logical inverses.
- **Card 5 vs 6:** Card 5 tests the factual difference, card 6 tests the reasoning for why SGD is preferred — different knowledge.
- **Back scan:** All backs are under 10 words. Card 5 has "and" but it's contrasting two variants in a single comparison, not two independent facts.
- **Distribution:** 5/8 are "why/how/reasoning" cards (~63%), 2 definitional, 1 formula. Meets the 40% target.

These look good? I'll push them to Anki. Edit any you'd like to change.
