

Here are your RSA flashcards:

| # | Type | Front / Text | Back / Extra | Tags |
|---|------|-------------|-------------|------|
| 1 | basic | What type of cryptography is RSA? | Asymmetric (public-key) cryptography | cryptography, rsa, definition |
| 2 | basic | RSA key generation: What is the first step? | Choose two large primes p and q | cryptography, rsa, key-generation |
| 3 | basic | RSA: How is the modulus n computed? | n = p × q | cryptography, rsa, key-generation, formula |
| 4 | basic | RSA: What function is used to compute φ(n), and what is the formula? | Euler's totient function: φ(n) = (p−1)(q−1) | cryptography, rsa, key-generation, formula |
| 5 | basic | RSA: What condition must the public exponent e satisfy relative to φ(n)? | gcd(e, φ(n)) = 1 (they must be coprime) | cryptography, rsa, key-generation |
| 6 | basic | RSA: What is the most commonly chosen value for the public exponent e? | 65537 | cryptography, rsa, key-generation |
| 7 | basic | RSA: How is the private exponent d computed? | As the modular inverse of e mod φ(n) | cryptography, rsa, key-generation |
| 8 | cloze | RSA: The public key is (n, {{c1::e}}) and the private key is (n, {{c2::d}}). | | cryptography, rsa, key-generation |
| 9 | cloze | RSA encryption: ciphertext = message^{{c1::e}} mod {{c2::n}} | | cryptography, rsa, formula |
| 10 | basic | RSA: What hard mathematical problem does RSA's security rely on? | Factoring the modulus n back into its prime factors p and q | cryptography, rsa, security |
| 11 | basic | RSA: What is the minimum recommended key size for security? | 2048 bits | cryptography, rsa, security |
| 12 | basic | Why is RSA rarely used to encrypt data directly? | It's too slow for bulk data | cryptography, rsa, application |
| 13 | basic | RSA: What is hybrid encryption and why is it used with RSA? | RSA encrypts a symmetric session key, which encrypts the actual data — combining RSA's key exchange with symmetric speed | cryptography, rsa, application |
| 14 | cloze | RSA digital signatures: sign with the {{c1::private}} key, verify with the {{c2::public}} key. | | cryptography, rsa, digital-signatures |

**Self-check notes:**
- Cards 8 and 14 are multi-cloze testing single unified concepts (not enumerations) — the key pair composition and the sign/verify relationship each form one concept.
- Cards 9 uses cloze because the formula reads naturally as a fill-in. I omitted a separate "decrypt formula" card since knowing encryption + which key decrypts trivially gives you the decrypt formula (mirror-deducible).
- Card 13's back is slightly long but it's a single "what + why" concept that would lose meaning if split further.
- Distribution: ~40% why/how/reasoning (cards 4, 5, 7, 10, 12, 13), ~30% core definitions/notation (1, 2, 3, 6, 8), ~20% application (11, 12, 13), ~10% process (9, 14).

These look good? I'll push them to Anki. Edit any you'd like to change.
