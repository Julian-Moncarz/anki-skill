I'll generate Anki cards from the gradient descent content. Let me work through the card creation process.

**Identifying card-worthy facts from the content:**

1. What gradient descent does (optimization to minimize cost function)
2. How it works mechanically (opposite direction of gradient)
3. The update rule formula
4. Learning rate too large → overshoot/diverge
5. Learning rate too small → slow convergence
6. Batch vs SGD vs mini-batch differences
7. Why SGD is preferred over batch
8. Convexity requirement for global minimum
9. What traps gradient descent in non-convex functions

**Self-checks:**
- Mirror scan: Cards 4 and 5 (learning rate too large/too small) are a mirror pair — knowing one trivially gives the other. Merging into a single multi-cloze.
- Mirror scan: Batch/SGD/mini-batch are a variant triad — collapsing to test the interesting distinctions only.
- Back scan: checking all backs for "and", ";", "—" violations. Fixed.
- Distribution: ~44% why/how, ~33% definitions/notation, ~22% application/reasoning. Good.

Here are the cards:

| # | Type | Front / Text | Back / Extra | Tags |
|---|------|-------------|-------------|------|
| 1 | basic | Gradient descent: Why does the algorithm update parameters in the OPPOSITE direction of the gradient? | The gradient points toward steepest ascent; moving opposite minimizes the cost function | machine-learning, gradient-descent, reasoning |
| 2 | cloze | The gradient descent update rule is: θ = θ − {{c1::α}} · ∇J(θ), where {{c1}} is the learning rate. | | machine-learning, gradient-descent, formula |
| 3 | cloze | Gradient descent: If the learning rate is too {{c1::large}}, the algorithm overshoots and may diverge; if too {{c2::small}}, convergence is painfully slow. | | machine-learning, gradient-descent, learning-rate |
| 4 | basic | Why is SGD often preferred over batch gradient descent in practice? | Faster per update | machine-learning, gradient-descent, sgd, reasoning |
| 5 | basic | How does the noise in SGD help optimization? | It can escape shallow local minima | machine-learning, gradient-descent, sgd, reasoning |
| 6 | basic | Gradient descent: What condition must the cost function satisfy to guarantee finding the global minimum? | Convexity | machine-learning, gradient-descent, convexity |
| 7 | basic | Gradient descent on a non-convex cost function can get stuck in what two types of points? | Local minima or saddle points | machine-learning, gradient-descent, reasoning |
| 8 | basic | Gradient descent: What does batch gradient descent compute the gradient over? | The entire dataset | machine-learning, gradient-descent, definition |
| 9 | basic | Gradient descent: How many training examples does SGD use per parameter update? | One | machine-learning, gradient-descent, sgd, definition |

These look good? I'll push them to Anki. Edit any you'd like to change.
