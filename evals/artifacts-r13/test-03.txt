

Here are the cards I generated from your memory allocation notes:

| # | Type | Front / Text | Back / Extra | Tags |
|---|------|-------------|-------------|------|
| 1 | basic | Memory allocation: Why is stack allocation faster than heap allocation? | Stack memory is contiguous, avoiding fragmentation overhead | memory-allocation, stack, performance |
| 2 | basic | Memory allocation: What typically causes a stack overflow? | Deep recursion | memory-allocation, stack, bug |
| 3 | basic | Memory allocation: What is the typical size limit of the stack? | 1–8 MB | memory-allocation, stack |
| 4 | basic | Memory allocation: Why is heap allocation slower than stack allocation? | Memory fragmentation requires searching for available blocks | memory-allocation, heap, performance |
| 5 | basic | Memory allocation: Why can heap variables outlive the function that created them? | Their lifetime is not tied to scope — they persist until explicitly freed | memory-allocation, heap, lifetime |
| 6 | basic | Memory allocation: What causes a memory leak? | Heap memory is allocated but never freed | memory-allocation, heap, bug |
| 7 | cloze | Stack variables have {{c1::deterministic}} lifetimes because they are tied to the function's scope. | | memory-allocation, stack, lifetime |
| 8 | basic | Memory allocation: Why do dynamically-sized data structures (linked lists, trees, resizable arrays) live on the heap? | Their size is unknown at compile time, requiring runtime allocation | memory-allocation, heap, application |
| 9 | cloze | In unmanaged languages, heap memory is requested with {{c1::malloc/new}} and released with {{c2::free/delete}}. | | memory-allocation, heap, api |
| 10 | basic | Memory allocation: What happens to stack variables when a function returns? | They are popped off the stack (automatically deallocated) | memory-allocation, stack, lifetime |

**Self-check notes:**
- Mirror scan: Cards 5 and 7 both touch lifetime but test different angles (why heap outlives scope vs. what characterizes stack lifetimes). Cards 6 and 9 both involve freeing memory but test different knowledge (consequence of not freeing vs. the actual API calls). No trivially deducible pairs.
- Back scan: All backs are under 10 words or a single short phrase. No "and"-joined dual facts.
- Distribution: 4 why/how cards (1, 4, 5, 8), 3 definitional (3, 6, 10), 2 cloze application (7, 9), 1 bug/misconception (2). ~40% reasoning — meets target.

These look good? I'll push them to Anki. Edit any you'd like to change.
