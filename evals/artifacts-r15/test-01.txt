Here are the Anki cards I generated from the gradient descent content:

| # | Type | Front / Text | Back / Extra | Tags |
|---|------|-------------|-------------|------|
| 1 | basic | Gradient descent: Why does the parameter update move in the *opposite* direction of the gradient? | The gradient points toward increasing cost; moving opposite minimizes it. | machine-learning, gradient-descent, reasoning |
| 2 | cloze | The gradient descent update rule is: θ = θ − {{c1::α}} · ∇J(θ) | | machine-learning, gradient-descent, formula |
| 3 | basic | Gradient descent: What happens if the learning rate α is too large? | The algorithm overshoots and may diverge. | machine-learning, gradient-descent, reasoning |
| 4 | basic | Gradient descent: What happens if the learning rate α is too small? | Convergence becomes painfully slow. | machine-learning, gradient-descent, reasoning |
| 5 | basic | How does SGD differ from batch gradient descent in what data it uses per update? | SGD uses one random example; batch uses the entire dataset. | machine-learning, gradient-descent, SGD |
| 6 | basic | Why is SGD often preferred over batch gradient descent despite being noisier? | Faster per update. | machine-learning, gradient-descent, SGD, reasoning |
| 7 | basic | How can the noise in SGD be beneficial for optimization? | It helps escape shallow local minima. | machine-learning, gradient-descent, SGD, reasoning |
| 8 | basic | Under what condition does gradient descent guarantee finding the global minimum? | When the cost function is convex. | machine-learning, gradient-descent, reasoning |
| 9 | basic | Gradient descent: Besides local minima, what other problematic points can the algorithm get stuck at? | Saddle points. | machine-learning, gradient-descent |

**Self-check notes:**
- **Mirror check:** Cards 3 & 4 (too large/too small α) could seem mirror-like, but they have genuinely different answers (divergence vs. slow convergence) — neither is deducible from the other.
- **Cards 6 & 7:** Split "faster and escapes minima" into two separate cards per the back-brevity rule.
- **Distribution:** 6/9 cards (~67%) are "why/how/what happens if" reasoning cards. Good balance.
- **No enumerations, no yes/no, no example-asking.**

These look good? I'll push them to Anki. Edit any you'd like to change.
